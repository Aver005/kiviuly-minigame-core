# PLANS
> Roadmap, active priorities, pending owner decisions. Last updated: 2026-07-24

## Как устроен план (единая нумерация — не путать с «фазами» в docs/)

Цель владельца: **сначала полностью объединить все три игры на ядре, потом
тестировать всё вместе**. Сетевые фичи — строго после объединения.

## [WIP] Переезд Escape — части A/B/C

- **A. Тулкит** — [DONE]
- **B. Арена** — [DONE]
- **C. Движок** — [WIP], четыре куска:
  1. Подсистемы (`RespawnBlocks`/`OfflineGuards`/`Themes`) — [DONE] косвенно: они
     держат `EscapeRules`, который уже переведён на матч.
  2. Правила: выпотрошить `GameSession`, добавить хуки — [DONE]
  3. **Перецелить вызывающие — [DONE].** Escape СОБИРАЕТСЯ на платформенном движке.
     Сделано: `ArenaManager` Escape → тонкий фасад над `core.arenas()` (не создаёт
     сессии, мостит `Match`→`EscapeRules`); `EscapeCommand` — на `plugin.arenas().leave/
     stop/forceStart` и `match.phase()`; `EscapeGame` зарегистрирован в MgCore, команда
     через `core.commandFor`; `ProtectionListener` удалён; `GameListener`/`ChatListener`
     оставлены (игро-специфика). `.all().values()`→`.all()`, `EscapeRules.Phase`→`GamePhase`.
  4. **Снос дубликатов и ревизия — [TODO] ← ТЕКУЩИЙ ШАГ.**
     - Удалить: `player/PlayerSnapshot`, `arena/SetupMarkers`, `arena/ArenaCheck`,
       `command/EscapeCommand` (команду ведёт `core.commandFor`). `arena/ArenaManager`
       уже схлопнут в фасад — при желании убрать и его, переведя ~94 вызова на `core.arenas()`.
     - Ревизия двойной обработки: escape `GameListener` и ядро оба слушают Bukkit-события;
       проверить урон/еду/блоки — не гасят ли друг друга (смерть/выход/снапшоты уже сняты).
     - Оффлайн-страж rejoin: у ядра фичи нет, сейчас только снапшот-восстановление ядра.
     - Статистика Escape (`StatsRepository`, свои колонки) — перевести в общий `StatsService`
       (нужны кастомные колонки в контракте) ИЛИ оставить своей осознанно.

## [TODO] После Escape

- **Унификация** — три игры к одному виду; обновить `docs/` (там уже разошлись
  `01-architecture`, `06-roadmap`, `07-escape-migration`: в них нет реестра игр,
  per-game папок арен, привязанных команд и прав).
- **Сквозной тест** — ядро + три игры на одном сервере: параллельные матчи разных игр,
  изоляция команд и прав, конфликт одинаковых id арен, откат мира, снапшоты после
  краша, статистика, меню разных плагинов через один `MenuListener` ядра.

## [TODO] Сетевые фичи (после объединения, не раньше)

Целевая топология — сеть за прокси: хаб с выбором игры + отдельный сервер на игру.
- Общая MySQL вместо локального SQLite → сквозная статистика (контракт `StatsService`
  уже готов, нужен второй бэкенд).
- `mg-hub`: селектор игр, сквозной профиль, транспорт игроков (`TransportService`
  и `QueueService` — контракты заложены, реализации нет; сейчас `LocalTransport` = no-op).
- Позже: `Team` (сейчас движок отдаёт FFA-заглушку), лидерборды-UI, лобби-PvP.

## [?] Открытые решения владельца

1. **Старые арены Escape** — формат сменился. Пересоздать руками или написать разовый
   переносчик со старого (папка `arenas/<ID>/` из 4 файлов) в новый? **Не решено.**
   Владелец сказал «парсер и прочее — потом», то есть решение отложено, но не отменено.
2. **`ChatChannel` Escape** — поднять в ядро как общую фичу или оставить игре? Пока у игры.
3. **`/mg` под строгим `mg.admin`** — владелец `sw.admin` не может через платформенную
   команду трогать даже свои арены. Оставить строго или смягчить (проверка владельца
   арены в `resolveArena`)? Решение принято «строго», но владелец может передумать.
