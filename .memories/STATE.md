# STATE
> Living snapshot — the most-updated file. Last updated: 2026-07-24

2026-07-24 — Escape: `EscapeRules` полностью переведён на платформенный матч
(внутри 0 ошибок); осталось перецелить вызывающие (16 ошибок в 5 файлах).

## Текущий фокус

**Переезд Escape на платформу, часть C (движок), кусок 3 из 4.**
Escape сейчас **НЕ СОБИРАЕТСЯ** — это ожидаемо и согласовано с владельцем
(«тест в конце, зелёный статус по ходу не нужен»). Платформа и две другие игры зелёные.

## Что работает [DONE]

- **Платформа** `mg-api` + `mg-core`: движок матча, реестр арен, тулкит, меню,
  статистика, реестр игр, команды с привязкой к игре, права. Собирается.
- **SkyWars** — тонкий плагин, свой `/sw`, право `sw.admin`. Собирается.
- **SkyBlockWars** — то же, `/sbw`, `sbw.admin`. Собирается.
- **Escape, часть A (тулкит)** — `Msg`/`Items`/`Keys`/`Menu` общие; игровые PDC-ключи
  вынесены в `EscapeKeys`, `pointAssistantCompass` — в `EscapeItems`.
- **Escape, часть B (арена)** — свой класс `Arena` удалён, работает на платформенном;
  числа и точки разложены по «карманам» через фасад `EscapeArena`; контент (киты,
  контракты, фразы смерти) — в `EscapeArenaConfig` + реестр `EscapeArenaConfigs`.
- **Escape, часть C, куски 1–2** — `GameSession` → `EscapeRules`, вырезано 19
  движковых методов, добавлены 11 хуков платформы. Внутри файла ошибок нет.

## Что сломано / в работе [WIP]

**Escape: 16 ошибок в 5 файлах** — вызывающие зовут удалённые методы движка
(`join`/`leave`/`forceStop`/`adminStop`/`finish`/старый `getPhase`):

| Файл | Ошибок | Суть |
|---|---|---|
| `arena/ArenaManager.java` | 4 | сам создаёт сессии, зовёт `join`/`forceStop` — теперь это работа ядра |
| `command/EscapeCommand.java` | 5 | `forceStart`/`adminStop`/фазы |
| `listener/GameListener.java` | 3 | движковые вызовы |
| `listener/ProtectionListener.java` | 1 | то же |
| `menu/ArenaSelectMenu.java` | 2 | то же |

Точный список: `cd E:/Projects/Me/escape-reborn && ./gradlew compileJava`.

## Проверено только компиляцией [?]

**Рантайм не проверялся вообще, ни одной игры.** Вся миграция подтверждена только
сборкой. Сквозной прогон на сервере — в самом конце (см. `PLANS.md`). Это осознанное
решение владельца, а не упущение.

## Последствия, которые всплывут при первом запуске [?]

- **Старые арены Escape не загрузятся** — формат сменился с папки из 4 файлов на
  `arenas/<ID>.yml` + `game/<ID>.yml`. Решение (пересоздать vs переносчик) **не принято**.
- **PDC-namespace общих ключей сменился** `escape:` → `mgcore:`: ранее выданные
  маркеры разметки и спец-предметы Escape перестанут распознаваться — перевыдать.
